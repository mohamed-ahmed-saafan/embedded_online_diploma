#include <stdio.h>
#include <string.h>

int main() {
   char str[100];
   char invstr[100];
   char ele;
   int count;
   printf("enter a string\n");
   fgets(str,100,stdin);
   count=strlen(str);
   for(int i=count;i>=0;i--){
       printf("%c",str[i]); 
       }
    return 0;
}